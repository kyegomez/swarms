{
    "name": "Swarms Project",
    "dockerComposeFile": "docker-compose.yml",
    "service": "app",
    "workspaceFolder": "/app",
    "forwardPorts": [
        8000
    ],
    "postCreateCommand": "pip install -r requirements.txt"
}
